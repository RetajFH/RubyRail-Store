<div class="pf-page">
  <p class="pf-eyebrow">Store / Editing #<%= @product.id %></p>
  <h1 class="pf-title">Edit product</h1>

  <%= render "form", product: @product %>
  <%= link_to "Cancel", @product, class: "pf-cancel" %>
</div>