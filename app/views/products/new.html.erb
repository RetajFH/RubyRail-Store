<div class="pf-page">
  <p class="pf-eyebrow">Store / New entry</p>
  <h1 class="pf-title">New product</h1>

  <%= render "form", product: @product %>
  <%= link_to "Cancel", products_path, class: "pf-cancel" %>
</div>